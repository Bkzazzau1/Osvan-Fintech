import 'package:flutter/material.dart';

class SharePaymentInfoScreen extends StatelessWidget {
  const SharePaymentInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Share Payment Info")));
  }
}
